; ESP32-2432S028R CYD + 2.8" ILI9341 (320x240 landscape)
; CYD port with XPT2046 touch zoom controls.

[env:cyd]

platform = espressif32@6.5.0

board = esp32dev

framework = arduino

monitor_speed = 115200

upload_port = COM8

monitor_port = COM8

board_build.partitions = partitions/plane_radar.csv

extra_scripts = post:scripts/merge_firmware.py

; Anti-aliased VLW font
board_build.embed_files = data/ui_font.vlw

build_flags =
  -std=gnu++17
  -DWM_NODEBUG
  -DWM_MDNS

lib_deps =
  lovyan03/LovyanGFX@^1.2.7
  tzapu/WiFiManager@^2.0.17
  bblanchon/ArduinoJson@^7.4.2
